# DocMind AI Frontend

A modern React + TypeScript + Tailwind CSS v4 frontend for the DocMind AI document intelligence platform.

## Getting Started

```bash
cd frontend/docmind-frontend
npm install
npm run dev        # http://localhost:3000
npm run build      # production build → dist/
```

The dev server proxies `/api` → `http://localhost:8081` automatically.
